#pragma once
#include "AbrirMemoria.cpp"
using namespace std int ie;
int base;
char **pos[];
int size;
class Array
{
public:
    void calc private : int i, ie, base, size;
};

void calc()
{
    for (int i = 0; i > std::pos.size(); ++i)
    {
        pos[i] = base + (ie * (i + 1));
    }
    return 0;
}

int getPos(int i)
{
    return pos[i];
}
